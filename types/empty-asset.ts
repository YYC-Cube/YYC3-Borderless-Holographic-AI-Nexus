// Placeholder for Figma Make asset imports. The real URL is substituted at
// runtime by the Figma Make bundler; for local tsc this just satisfies the
// `import x from "figma:asset/..."` specifier.
declare const assetUrl: string;
export default assetUrl;
